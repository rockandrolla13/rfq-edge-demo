[project]
name = "rfq-edge"
version = "0.1.0"
description = "Edge-consistent RFQ responder demo"
requires-python = ">=3.11"
dependencies = [
    "matplotlib>=3.8",
    "numpy>=1.26",
    "pandas>=2.1",
    "scikit-learn>=1.4",
]

[project.optional-dependencies]
demo = [
    "ipykernel>=6.29",
    "jupyter_client>=8.6",
    "nbconvert>=7.16",
    "nbformat>=5.10",
]
dev = [
    "pytest>=8.0",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
